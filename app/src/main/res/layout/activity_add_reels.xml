<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" >

    <data>

        <variable
            name="handler"
            type="com.undefinedparticle.instagramclone.activities.AddReelsActivity.ClickHandler" />

        <import type="android.view.View" />

    </data>

    <LinearLayout
        style="@style/settingsBG"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/settingsBG"
            android:paddingStart="17dp"
            android:paddingTop="14dp"
            android:paddingEnd="23dp"
            android:paddingBottom="14dp">

            <ImageView
                android:layout_width="26dp"
                android:layout_height="26dp"
                android:onClick="@{handler::goToBack}"
                android:layout_centerVertical="true"
                app:tint="@color/settingsImageColor"
                android:src="@drawable/left_arrow" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:text="Create Reels"
                android:textAlignment="center"
                android:textColor="@color/settingsImageColor"
                android:textSize="18sp"
                android:textStyle="bold" />

        </RelativeLayout>

        <View
            android:id="@+id/dividerView"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/viewLineColor"
            app:layout_constraintBottom_toBottomOf="parent" />





    </LinearLayout>

</layout>